name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  tests:
    name: Run Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: FraganceSuite/Source_code/ProjectAroma

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: ""
          MYSQL_ALLOW_EMPTY_PASSWORD: yes
          MYSQL_DATABASE: aroma_test
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=3

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup PHP 8.2
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring, pdo, pdo_mysql, xml, zip

      - name: Copy .env
        run: cp .env.example .env

      - name: Set database environment variables
        run: |
          grep -v "^DB_CONNECTION\|^DB_HOST\|^DB_PORT\|^DB_DATABASE\|^DB_USERNAME\|^DB_PASSWORD" .env > .env.tmp && mv .env.tmp .env
          echo "DB_CONNECTION=mysql" >> .env
          echo "DB_HOST=127.0.0.1" >> .env
          echo "DB_PORT=3306" >> .env
          echo "DB_DATABASE=aroma_test" >> .env
          echo "DB_USERNAME=root" >> .env
          echo "DB_PASSWORD=" >> .env

      - name: Install Composer dependencies
        run: composer install --no-interaction --prefer-dist

      - name: Generate application key
        run: php artisan key:generate

      - name: Import database schema
        run: mysql -h 127.0.0.1 -u root --password="" aroma_test < database/schema.sql

      - name: Create Vite manifest
        run: |
          mkdir -p public/build
          echo '{
            "resources/js/app.js": {"file": "assets/app.js", "src": "resources/js/app.js"},
            "resources/js/favorites.js": {"file": "assets/favorites.js", "src": "resources/js/favorites.js"},
            "resources/css/app.css": {"file": "assets/app.css", "src": "resources/css/app.css"}
          }' > public/build/manifest.json

      - name: Run tests
        run: |
          php artisan test \
            tests/Feature/VideoTest.php \
            tests/Feature/DiscountPriceTest.php \
            tests/Feature/OrderHistoryTest.php \
            tests/Feature/BestSellerTest.php \
            tests/Feature/DashboardGraphsTest.php \
            tests/Feature/ReviewModerationTest.php \
            tests/Unit/BestSellersLogicTest.php \
            tests/Unit/InfoCarouselTest.php \
            tests/Unit/VideoHelperTest.php \
            --stop-on-failure

  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: tests
    if: github.ref == 'refs/heads/main' && success()

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          port: ${{ secrets.SSH_PORT }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/aromuwjs/devops.aromaperfumeria.shop
            git pull origin main
            cd Source_code/ProjectAroma
            composer install --no-interaction --prefer-dist --optimize-autoloader
            php artisan config:cache
            php artisan route:cache
            php artisan view:cache

      - name: Send success email
        if: success()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: "[AromaSuite] Pipeline PASSED - ${{ github.ref_name }}"
          to: ${{ secrets.MAIL_USERNAME }}
          from: ${{ secrets.MAIL_USERNAME }}
          body: |
            Pipeline completed successfully.

            Repository: ${{ github.repository }}
            Branch: ${{ github.ref_name }}
            Commit: ${{ github.sha }}
            Triggered by: ${{ github.actor }}

      - name: Send failure email
        if: failure()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: "[AromaSuite] Pipeline FAILED - ${{ github.ref_name }}"
          to: ${{ secrets.MAIL_USERNAME }}
          from: ${{ secrets.MAIL_USERNAME }}
          body: |
            Pipeline failed. Check the Actions tab for details.

            Repository: ${{ github.repository }}
            Branch: ${{ github.ref_name }}
            Commit: ${{ github.sha }}
            Triggered by: ${{ github.actor }}
